#include"Header3.h"

int main()
{
    double a = 2, b = 3, d = b - a, e = 10e-10; //[a,b] interval ,d interval span ,e precision 
    if (auto c = dicection(a, b, f<long double>); c.has_value()) {
        std::cout << *c << '\n';
    }
    return 0;
    

}
